---
description: Pick up where the last session left off, or write the handoff for the next one
argument-hint: "[read | write]"
---

Follow the instructions in `.claude/skills/handoff/SKILL.md` in this repository.

Read that file first, decide between read mode and write mode exactly as it says,
and carry out that mode's steps.

Arguments (may be empty): $ARGUMENTS
