---
description: Release LiveConsole Ops to live.kriviinfotech.com and verify the deployed build
argument-hint: "[skip-db | dry-run | no-commit]"
---

Follow the instructions in `.claude/skills/deploy/SKILL.md` in this repository.

Read that file first, then work through its steps in order — preflight, commit
and push, `npm run deploy`, and the verification it describes. Do not skip the
verification step or report a deploy as done without it.

Arguments (may be empty): $ARGUMENTS
